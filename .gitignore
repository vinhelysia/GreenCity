# Dependencies
node_modules/
.pnpm-store/

# Builds
dist/
build/
.next/
out/
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local

# Logs / coverage
logs/
*.log
coverage/
.nyc_output/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Prisma
apps/api/prisma/*.db
apps/api/prisma/*.db-journal

# Test / tooling
playwright-report/
test-results/
.turbo/

# Misc
*.pem
.vercel

# Local runtime data (storage, mail, tooling downloads)
.local/
# Anchored: unanchored `data/` also matched apps/web/src/data, which silently
# kept committed source out of the repo and broke the build on deploy.
/data/
uploads/
tmp/
temp/

# User-owned local tooling — never part of the product repo
.agents/
blender/

# Demo photographs are third-party press images: kept out of the public repo,
# uploaded to object storage by the seed. Without them the seed falls back to
# generated placeholders, so a fresh clone still seeds.
apps/api/prisma/seed-assets/
